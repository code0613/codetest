class Solution {
    public String solution(String[] arr) {
        StringBuilder anw = new StringBuilder();
        for(int i=0;i<arr.length;i++) {
            anw.append(arr[i]);
        }
        return anw.toString();
    }
}